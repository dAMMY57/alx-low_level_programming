#include "main.h"
/**
 * _strlen - prints the length of a string.
 *@s: The input string
 *
 *Return: the length of the string
 */
int _strlen(char *s)
{
int length;
for (length = 0; s[length] != '\0'; length++)
{

}
return (length);
}
